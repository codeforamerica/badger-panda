/**
 * Example settings file (since Dotcloud doesnt really
 * handle environment variables in a way that it doesn't need
 * to be committed in repo).
 */
exports.db = {
  'host': 'some_value',
  'user': 'some_value',
  'pass': 'some_value',
  'port': 0000,
  'name': 'some_value'
};


exports.throttle = true;